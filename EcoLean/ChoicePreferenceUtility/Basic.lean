import EcoLean.ChoicePreferenceUtility.Preference

namespace EcoLean

/-!
# Basic compatibility import for preference theory

This file is a compatibility shim.

The canonical preference API lives in
`EcoLean.ChoicePreferenceUtility.Preference`.
Import this file only when maintaining older code that still refers to the
historical `Basic` path.
-/

end EcoLean
